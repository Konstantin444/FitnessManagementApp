@model FitnessApp.Models.Trainer

@{
    ViewData["Title"] = "Edit Trainer";
}

<h1 class="text-center form-title">Edit Trainer</h1>

<div class="d-flex justify-content-center align-items-center edit-form-wrapper">
    <form asp-action="EditTrainer" method="post" class="edit-session-form">
        <!-- Trainer Name -->
        <div class="form-group mb-3">
            <label asp-for="Name" class="form-label">Trainer Name</label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <!-- Actions -->
        <div class="d-flex justify-content-between">
            <button type="submit" class="btn btn-primary">Save</button>
            <a asp-action="Dashboard" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>

<link rel="stylesheet" href="~/css/edit-trainer.css" asp-append-version="true" />

